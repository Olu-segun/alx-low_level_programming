#include <stdio.h>
/**
 * main - Print alphabet using putchar
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp = 'a';

	while (apl <= 'z')
	{
		putchar(apl);
		++apl;
	}
	putchar('\n');
	return (0);
}
